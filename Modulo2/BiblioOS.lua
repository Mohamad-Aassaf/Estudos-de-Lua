print(os.date()) -- mostra a data que está no sistema

local data = os.date()
print(type(data)) -- tipo String

os.rename("Arquivo.txt", "MeuArquivo.txt") -- Muda o nome do arquivo!

 -- os.remove() -- Remove o arquivo selecionado
 os.execute() --executa um comando no terminal

 -- BIBLIOTECA OS
 -- http://lua-users.org/wiki/OsLibraryTutorial